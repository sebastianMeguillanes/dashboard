
  # Database Schema Setup

  This is a code bundle for Database Schema Setup. The original project is available at https://www.figma.com/design/NakScVkLjP1lDUHZ99FwMf/Database-Schema-Setup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  